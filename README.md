# Atomic Wrapper for DynamoDB

Atomic Wrapper exposes a very simple interface to work with DynamoDB.
The main benefit is that the same interface can be used to work with S3, DenoKV and other databases.
So any apps created usingthis interface become easily portable to other database engines.



